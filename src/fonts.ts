// Self-hosted fonts via @fontsource (Vite bundles the woff2 files).
// Replaces the Google Fonts CDN @import previously in style.css.

// Inter — terminal-trailer skip + quote scenes (only 500 weight used)
import '@fontsource/inter/500.css'

// JetBrains Mono — terminal-trailer scene panel (default weight)
import '@fontsource/jetbrains-mono/400.css'
import '@fontsource/jetbrains-mono/500.css'

// IBM Plex Mono — matrix-rain canvas + pretext abstract
import '@fontsource/ibm-plex-mono/400.css'

// Instrument Sans — primary UI font (paper page body, headings, links)
import '@fontsource/instrument-sans/400.css'
import '@fontsource/instrument-sans/500.css'
import '@fontsource/instrument-sans/600.css'
